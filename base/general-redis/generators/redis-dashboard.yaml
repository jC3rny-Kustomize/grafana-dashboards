---

apiVersion: builtin
kind: ConfigMapGenerator
metadata:
  name: -redis-dashboard

options:
  labels: {}
  annotations: {}

  disableNameSuffixHash: true

files:
  - redis-dashboard.json=./files/redis-dashboard.json
